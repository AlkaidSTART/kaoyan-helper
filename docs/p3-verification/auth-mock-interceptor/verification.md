# P3 - 登录拦截与 Mock 登录验证

## 1. 测试用例执行结果
- [x] **默认拦截**：未经认证直接启动应用时，界面严格呈现在登录页。
- [x] **快速填入**：点击“填入测试账号”快速预置测试数据（`13800000000` / `123456`）并自动勾选协议。
- [x] **Mock 登录**：点击登录后通过 Mock 校验，成功加载 `UserModel.mock` 并进入主看板。
- [x] **数据注入**：主页面顶部动态展示目标院校（`浙江大学 · 计算机 (085404)`）与倒计时（`距考研 98 天`）。
- [x] **登出拦截**：在主界面点击头像弹出菜单选择“退出登录”，安全清空凭证并平滑重定向回登录界面。

## 2. 静态检查
- [x] `flutter analyze` 零警告。
- [x] 无任何系统 Emoji，无 `print` 语句。

## 3. 性能与多端验收单
- [x] 动效 <= 400ms，状态流转无闪屏。
- [x] 自动化测试用例全量通过：
  ```
  Analyzing kaoyan_helper...
  No issues found! (ran in 1.8s)
  00:02 +6: All tests passed!
  ```
